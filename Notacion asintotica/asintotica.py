# Ley de la suma
def f(n):
    for i in range(n):
        print(i)
    
    for i in range(n)
        print(i)

# O(n) + O(n) = O(n + n) = O(2n) = 0(n)
# Big o de N

# Loop sencillo = crecimiento lineal

# Ley de la multiplicacion

def f(n):
    for i in range(n):
        for i in range(n):
            print(i, j)
# O(n) * O(n) = O(n * n) = O(n``2)

# Loop dentro de otro loop = crecimiento cuadratico

# Recursividad multiple

def fibonacci(n)
    if n == 0 or n == 1:
        return 1
    return fibonacci(n-1) + fibonacci(n-2)
    # O(2**n)
# Funcion recursiva que genera 3-4 llamadas recursivas = crecimiento exponencial
